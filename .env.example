# Copy this file to .env and fill in real values. .env is never sent to the browser
# and should never be committed to git or shared with anyone.

# Generate both of these by running: npm run hash-password
JWT_SECRET=
ADMIN_PASSWORD_HASH=

PORT=3000
NODE_ENV=development
